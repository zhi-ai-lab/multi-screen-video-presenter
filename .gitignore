# Folder-level ignores only — keep individual files out of this list.
# Drop anything machine-specific or private in here (shortcuts, personal
# videos, scratch scripts). The folder name shields local file names too.
local/

# nginx binary distribution (the unpacked release folder, not our source).
nginx/
